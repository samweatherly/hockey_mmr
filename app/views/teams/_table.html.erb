<table class='table table-striped'>
  <thead>
    <tr>
      <th></th>
      <th>Team</th>
      <th>MMR</th>
    </tr>
  </thead>
  <tbody>
    <% @filter.each do |t| %>
      <tr>
        <td><%= image_tag(t.logo.url(:teeny)) %></td>
        <td><%= t.name %></td>
        <td><%= t.mmr.round(0) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
